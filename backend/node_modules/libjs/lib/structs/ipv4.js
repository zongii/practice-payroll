"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const typebase_1 = require("../typebase");
const ipv4 = typebase_1.Type.define(4, function (offset = 0) {
    const buf = this;
    const socket = require('../../socket');
    const octets = socket.Ipv4.type.unpack(buf, offset);
    return new socket.Ipv4(octets);
}, function (data, offset = 0) {
    const buf = this;
    data.toBuffer().copy(buf, offset);
});
exports.default = ipv4;
