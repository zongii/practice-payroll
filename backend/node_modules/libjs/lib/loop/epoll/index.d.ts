import { Loop } from "../Loop";
export declare class EpollLoop implements Loop {
    maxEvents: number;
    epfd: number;
    constructor(maxEvents?: number);
    listen(fd: any, callback: any): number;
    run(): void;
    asyscall: () => void;
}
